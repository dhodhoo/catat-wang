services:
  waha:
    image: devlikeapro/waha:latest
    container_name: catatuang-waha
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      WAHA_API_KEY: ${WAHA_API_KEY}
    volumes:
      - ./.waha:/app/.sessions
